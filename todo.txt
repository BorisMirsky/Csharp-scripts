
Темы взяты из курса 'C# продвинутый' https://stepik.org/course/288694/promo.
Разбираю их с примерами. Приоритет темам, которые можно сделать в .csx скрипте. Остальные пока что откладываю.



1) done:

Память без аллокаций: Span, Memory, ArrayPool, ref struct, stackalloc, паттерны zero-allocation.

Многопоточность: TPL, lock-free структуры, System.Threading.Channels, паттерны producer/consumer и pipeline, и ещё несколько тем.

Метапрограммирование: Reflection, Expression Trees.

Асинхронность: state machine async/await, Task vs ValueTask, ConfigureAwait, синхронизационные контексты, отмена и таймауты.

Производительность и профилирование: BenchmarkDotNet, dotTrace, PerfView, ETW, чтение JIT-вывода (не всё можно реализовать через .csx)




2) todo:

EF Core продвинутый: оптимизация запросов, миграции, проблема N+1, правильная работа с DbContext.

Production: Observability через OpenTelemetry, диагностика дампов, health-чеки, надёжность.

Тестирование: unit, integration, contract тесты, моки, in-memory БД.

Внутренности CLR: загрузка сборок, JIT и AOT, GC и поколения, модель памяти, layout объектов.
                  Часть можно сделать с .csx (для некоторых с разрешением unsafe code), для AOT надо будет создать проект. Но вообще тут всё сложно.

Метапрограммирование: Source Generators и Roslyn API для генерации кода во время компиляции. Тут нужны не скрипты .csx, а отдельные проекты.



3) Многое из списка ниже делал либо применял, проекты в репозитории. Остальное todo.

Архитектура: Clean Architecture, DDD, CQRS, Event Sourcing на примере реального сервиса.   

Микросервисы: gRPC, MassTransit, Saga, Outbox, идемпотентность.

Безопасность: аутентификация, OWASP API Top 10, защита от типичных атак на .NET-приложения.

